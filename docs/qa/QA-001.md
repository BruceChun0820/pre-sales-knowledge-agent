# QA Report — PR-2

## Tested Commit

- Commit: `21f5f91fe563e87485e6eae6a50b4a6064d0267a`
- Source branch: `feature/phase-01-ingestion-qdrant`
- Target branch: `dev`
- Related tasks: Phase 1 / TASK-07, TASK-08; QA-001 issue remediation
- PR: [#2 — Phase 01 Document Ingestion + Vector DB](https://github.com/BruceChun0820/pre-sales-knowledge-agent/pull/2)
- PR state: open, not merged; PR head and remote source branch match the tested SHA. GitHub reports no configured status checks.
- The only pre-existing worktree change is the untracked QA report `docs/qa/QA-001.md`; no Dev source changes were present outside the committed head.

## Environment

- Python: 3.14.4
- OS: Ubuntu 26.04.1 LTS
- Docker Engine / Compose: 29.8.1 / v5.5.1
- Qdrant: `qdrant/qdrant:v1.19.1`, healthy at `127.0.0.1:6333`
- Dependency lock SHA-256: `9e8a9707ebc3aa3bc967bf765033bbcc40a1fbb9640f5122ffd3c13ea3544c93`
- Embedding checks used the pinned local model cache offline; no hosted model/API calls.

## Acceptance Criteria Results

| Criterion | Result | Evidence |
|---|---|---|
| AC-P1-01 | PASS | Existing isolated environment installed from committed `requirements-phase1.lock` was used; `pip check` reports no broken requirements, and the same clean environment ran all 54 tests. Lock hash matches the documented baseline. |
| AC-P1-02 | PASS | Current-head full suite passes parser golden tests for PDF, DOCX, Markdown, and TXT, including ordered content and provenance. |
| AC-P1-03 | PASS | Current-head full suite passes corrupt/unsupported/path-escape and per-document isolation tests; valid documents remain ingestible in mixed batches. |
| AC-P1-04 | PASS | Current-head full suite passes deterministic chunk identity, checksum, required source locator, version, index, and non-empty-text tests. |
| AC-P1-05 | PASS | Current-head full suite passes cleaning/chunking preservation tests for important numbers, product/model terms, negation, and limitations. |
| AC-P1-06 | PASS | Live CLI: first run `discovered=4, parsed=4, chunked=4, embedded=4, upserted=4, skipped=0, failed=0`; identical rerun `skipped=4, upserted=0, failed=0`. Manifest records=4 and live Qdrant points=4. QA collection was deleted after verification. |
| AC-P1-07 | PASS | Current-head integration tests pass changed-content versioning and failed replacement safety; metadata-only embedding failure was also injected against live Qdrant and left the prior active payload searchable with exactly one active point. |
| AC-P1-08 | PASS | Reviewed benchmark report compares two candidates over the same 12 reviewed queries and 12 chunks, with quality, latency, throughput, memory, dimension, and index-size metrics. Both candidates succeeded. |
| AC-P1-09 | PASS | Decision record selects pinned multilingual MiniLM. The selected-rerun verifier passes at tolerance 0.01, dimension 384, and zero Hit Rate@5 / Recall@10 deltas. |
| AC-P1-10 | PASS | Current-head integration suite includes live Qdrant filter/upsert and Compose restart-persistence tests; both passed within the 54-test run. Qdrant remained healthy after the test. |
| AC-P1-11 | PASS | Live Qdrant metadata-only reclassification: same bytes changed from finance to healthcare; result `first_upserted=1, changed_upserted=1, unchanged_skipped=1, finance=0, healthcare=1, active=1`. Thus filters return no stale classification and no duplicate active point. |
| AC-P1-12 | PASS | Current-head adapter tests pass wrong-dimension/model and incompatible collection configuration rejection cases; no incompatible vector is silently activated. |
| AC-P1-13 | PASS | Live CLI counts reconcile on both passes; first-run summary is four successful documents, and exact manifest/Qdrant counts are both four. |
| AC-P1-14 | PASS | `.venv/bin/pytest -q`: 54 passed, 0 failed/errors. Ruff lint passes; Ruff format check: 59 files already formatted. The lock-installed isolated environment also reports 54 passed. Compose config validates. |
| AC-P1-15 | PASS | Reviewed tracked submission paths and commit changes: no credentials, private customer/company documents, model binaries, or Qdrant runtime storage detected. Fixtures are synthetic. The only grep warning is an expected payload-index warning from in-memory Qdrant in unit tests. |
| AC-P1-16 | PASS | Reviewed PR diff, dependency baseline, domain boundaries, and Phase scope: no LLM, FastAPI, Agent framework, OCR, hybrid retrieval, reranker, or out-of-scope infrastructure added. Qdrant remains the sole new service as specified. |
| AC-P1-17 | PASS | GitHub PR #2 is reachable/open, targets `dev`, source is the stated feature branch, and remote branch/PR head both equal tested SHA `21f5f91fe563e87485e6eae6a50b4a6064d0267a`; no self-merge occurred. |

## Automated Tests

- `.venv/bin/pytest -q` — 54 passed; one expected warning that in-memory Qdrant does not apply payload indexes.
- `/tmp/phase1-qa-clean-tqTFJM/.venv/bin/python -m pytest -q` — 54 passed; same expected warning.
- `.venv/bin/ruff check .` — passed.
- `.venv/bin/ruff format --check .` — 59 files already formatted.
- `.venv/bin/python -m pip check` — no broken requirements.
- `docker compose -f infra/docker-compose.yml config --quiet` — exit 0.
- `.venv/bin/python scripts/verify_embedding_rerun.py --benchmark evaluation/reports/TASK-05-embedding-benchmark.json --rerun evaluation/reports/TASK-05-selected-rerun.json` — passed; 384 dimensions, metric deltas 0.0, tolerance 0.01.
- GitHub status checks — none configured.

## Functional Tests

- Re-ran sample ingestion through the real embedding provider and real Qdrant with pinned model revision, using a unique QA collection and temporary manifest. The two CLI summaries and reconciled counts are recorded under AC-P1-06/13.
- Ran metadata-only reclassification against live Qdrant using identical file bytes and explicit metadata changes. Healthcare filter count became 1, finance became 0, active count remained 1.
- Ran live-Qdrant negative filter/control case for failed metadata replacement: injected embedding failure returned one failure while the previous healthcare payload remained active/searchable; the proposed finance payload remained absent and active count remained 1.
- Current-head persistence integration test restarted the Compose Qdrant service and confirmed the test point remained queryable.

## Negative / Edge Cases

- Automated negative/edge tests passed for corrupt and unsupported files, path/symlink escape, invalid model/dimension, failed content-version replacement, and failed metadata-only replacement.
- Live metadata update failure safety is recorded under AC-P1-07/11.
- Normal unchanged rerun remained idempotent after a metadata-only change: one active point, one skipped document, zero extra upserts.

## Regression

No regression found in the full Phase 1 suite or live CLI/Qdrant smoke tests. The previously failing AC-P1-11 scenario now passes against the real Qdrant service. Phase status and Dev Evidence now accurately say READY FOR QA RETEST and do not claim QA PASSED.

## Code Quality

- Lint / format / tests: PASS.
- Reviewed fix and regression tests in `pipeline.py`, `qdrant.py`, and `test_ingestion_pipeline.py`; deterministic point identity is retained, metadata comparison triggers reindexing, and failed embedding leaves the existing active payload unchanged.
- Phase status and Dev evidence identify the remediation and verification; docs do not claim QA approval.
- No unresolved maintainability, scope, dependency, or security issue found in the reviewed changes.

## Issues

No unresolved issues found in this retest. The prior AC-P1-11 issue and stale Phase/Evidence status have been addressed in the tested commit.

## Final Decision

QA PASSED — all mandatory AC-P1-01 through AC-P1-17 pass on commit `21f5f91fe563e87485e6eae6a50b4a6064d0267a`. This is QA approval evidence only; PR #2 remains open and unmerged pending PM scope/architecture review and PM/maintainer merge to `dev`. No `dev → main` promotion is authorized by this report.
