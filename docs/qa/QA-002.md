# QA Report — PR-9 / P2-TASK-01

## Tested Commit

- Commit: `ffc487b6d7b7a5dd752dbc72024c2db889bb947a`
- Source branch: `feature/phase-02-contracts`
- Target branch: `main`
- PR: [#9 — Freeze Phase 2 contracts and test doubles](https://github.com/BruceChun0820/pre-sales-knowledge-agent/pull/9)
- Base SHA: `6a5da8782701bdd59a0b2ae07dede273b416a488`
- Remote state: open, not merged, not draft; PR base/head and remote refs match these values. GitHub reports no configured status checks.
- Tested checkout was clean before QA. The commit range from the base changes only `app/domain/contracts.py`, `app/domain/fakes.py`, `tests/unit/test_phase2_contracts.py`, and `docs/handoffs/P2-TASK-01.md`.

## Environment

- Host: `ubuntu-dev-tailscale`, Ubuntu 26.04.1 LTS
- Python: 3.14.4
- Pytest: 8.4.2
- Ruff: 0.16.8
- Environment: isolated project `.venv`, previously set up from the existing Phase 1 lock as documented; `pip check` reports no broken requirements.
- No new dependency/lock changes in the tested PR.

## Acceptance Criteria Results

| Criterion | Result | Evidence |
|---|---|---|
| Invalid/empty query, invalid top-k, unknown enum, malformed or unauthorized filters raise stable typed validation errors | PASS | Re-ran `tests/unit/test_phase2_contracts.py`: 19 passed. Parameterized tests assert Pydantic `ValidationError` for blank query, top_k 0/101, unknown document type, malformed labels, tenant/active override, and unknown filter. |
| Search hits serialize rank, score, chunk/document/version identity, and source location without framework objects | PASS | `test_search_hit_serializes_rank_score_identity_and_source_location` passes; `SearchHit.from_chunk` copies the Phase 1 domain fields and JSON serialization is asserted. |
| Query responses serialize answered, insufficient_evidence, and failed statuses with claims, citations, warnings, metrics, and request ID | PASS | `test_query_response_serializes_each_terminal_status` validates and serializes all three terminal states; adjacent tests reject inconsistent status/answer/failure and usage data. |
| Contract modules import when FastAPI, Qdrant, OpenAI SDK, LangChain, and LangGraph imports are blocked | PASS | `test_contract_import_succeeds_when_infrastructure_and_provider_sdks_are_blocked` inserts an import blocker before importing contracts/fakes and passes in a subprocess. |
| Deterministic fakes cover retrieval success/empty/failure and LLM success/malformed/failure | PASS | Fake search test verifies byte-equivalent serialized repeated results and all three outcomes; parameterized fake LLM tests verify success, malformed JSON-shaped output, and typed provider failure. |

## Automated Tests

- `.venv/bin/python -m pytest tests/unit/test_phase2_contracts.py -q` — **19 passed**.
- `./init.sh` — **73 passed**, Ruff lint passed, Ruff format check passed (73 files), harness completed.
- `git diff --check 6a5da8782701bdd59a0b2ae07dede273b416a488...HEAD` — passed for the committed PR diff.
- `.venv/bin/python -m pip check` — no broken requirements.
- GitHub CI/status checks — none configured.

## Functional / Negative Tests

- Request validation exercised blank/whitespace query, top-k lower/upper bound violations, unknown enum, malformed filter values, and attempts to set system-owned or unknown filters.
- Serialization exercised all three query terminal statuses and source-aware hit identity.
- Import isolation blocked FastAPI, Qdrant, OpenAI, LangChain, and LangGraph before loading the new contract and fake modules.
- Fakes exercised stable repeated retrieval output, empty results, retrieval failure, successful generation, malformed provider output, and provider failure.
- No real retrieval, provider network call, endpoint, or dependency was introduced, consistent with P2-TASK-01 scope.

## Regression and Code Quality

- Full existing suite passes; no regression observed.
- Diff review confirms only the four scoped files above changed relative to the declared base. No dependency or lock file changed.
- Domain contracts use Phase 1 domain types and standard/Pydantic types; the import-blocker test confirms the new module boundary does not pull forbidden infrastructure/provider SDKs.
- Ruff lint and formatting pass. Committed diff whitespace check passes.
- In-memory Qdrant emits the existing payload-index warning in the full suite; this does not fail tests and is unrelated to the new contract code.

## Issues

- LOW / local QA environment only: local branch `feature/phase-02-contracts` is configured to track `origin/main`, and local `main` is stale (`ad9c4a8…`). The authoritative `origin/main` is the declared base `6a5da878…`, and `origin/feature/phase-02-contracts` exactly matches the tested head. No PR/source mismatch resulted, but correct the local upstream configuration before future pull/push operations to reduce operator error.
- No acceptance-blocking or PR-code issue found.

## Final Decision

PASS — all five mandatory P2-TASK-01 acceptance criteria pass on PR head `ffc487b6d7b7a5dd752dbc72024c2db889bb947a`. This is QA evidence, not merge authorization: PR #9 remains open and unmerged pending PM review/approval and maintainer merge. Dev2 remains read-only until that gate is completed; record the exact resulting `main` SHA before retrieval work starts.

## Post-QA Merge Record

- PM reviewed the scoped change and merged PR #9 on 2026-09-23.
- Merge commit on `main`: `c6a65e5c06966124eeddf0e55bef71998dafac58`.
- This commit is the contract-gate baseline for Dev1 and Dev2 implementation work.
- The upstream configuration observation was corrected locally for `feature/phase-02-contracts`; it now tracks `origin/feature/phase-02-contracts`.
